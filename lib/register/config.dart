class AppConfig {
  // Change this to the desired base URL for your server.
  // Example: Replace `localhost` with your production or ngrok URL for testing on devices.
  static const String baseUrl = "http://192.168.8.113/yoga_app";
}
